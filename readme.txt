c# 高效读写文件，本机测试，200M文件2134毫秒完成读写
   1.内存映射方式读写文件（写测试报权限错误）
   2.FileStream类读写文件（2种方式写，直接写入和缓存写，调整了FileStream内部缓存大小）
   3.实现了读取文件的缓存，默认每个buffer大小5M，初始化10个buffer,也就是50M缓存读取
   4.FileStream缓存写入，默认开启接收，写入复制各20M大小。
   
   根据上面说明，当前测试需要的初始化化缓存共90M。可以根据自己需要调整缓存大小。
   
